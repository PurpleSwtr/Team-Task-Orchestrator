export interface UserData {
  id: string;
  gender: string;
  // firstName: string;
  // secondName: string;
  // lastName: string | null; 
  shortName: string;
  role: Array<[]>;
  email: string | null; 
}